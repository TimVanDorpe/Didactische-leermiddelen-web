@model Groep9.NET.ViewModels.LogInViewModel
@{
    ViewBag.Title = "Log In";
}

<h2>Log In</h2>

@Html.ValidationSummary(true)

@using (Html.BeginForm()) {
    @Html.EditorForModel()
    <p>
        <br/>
        <button type="submit">Log In</button>
    </p>
}