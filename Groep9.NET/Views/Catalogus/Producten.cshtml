@using Groep9.NET.Controllers
@using Groep9.NET.Models.Domein
@model IEnumerable<Groep9.NET.ViewModels.ProductViewModel>

    


<div class="hidden-sm hidden-xs">
    <table class="table table-responsive ">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Foto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Naam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Omschrijving)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AantalBeschikbaar)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Uitleenbaarheid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plaats)
            </th>
                  
            <th></th>
        </tr>
        @if (!Model.Any())
        {
            <tr>
                <td colspan="7">Er zijn geen materialen gevonden die voldoen aan de zoekcriteria.</td>
            </tr>

        }
        @foreach (var item in Model)
        {
            <tr>
                <td>
                   
                    <img style="width: 75px; height: 75px;" alt="@Html.DisplayFor(modelItem => item.Naam)" src="~/Content/images/@(@Html.DisplayFor(modelItem =>item.Foto))" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Naam)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Omschrijving)
                </td>
                
                <td>
                    @Html.DisplayFor(modelItem => item.AantalBeschikbaar)
                </td>
              
                <td>
                    @Html.DisplayFor(modelItem => item.Uitleenbaarheid)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Plaats)
                </td>
              
                <td>

                  @Html.ActionLink("Details", "Details", new { id = item.ProductId })
                    @*

                     @using (Html.BeginForm("AddOfVerwijderVerlanglijst", "Catalogus", FormMethod.Post, new {id = item.ProductId, @class = "radioform" }))
                     {
                         <div class="" data-toggle="buttons">
                             <label class="btn btn-lg btn-danger">
                                 <input type="radio" name="actie" id="verwijderen" value="verwijderen" autocomplete="off" class="radio">
                                 <i class="glyphicon glyphicon-remove-circle"></i>
                             </label>
                             <label class="btn btn-lg btn-default active">
                                 <input type="radio" name="actie" id="toevoegen" value="toevoegen" autocomplete="off" class="radio">
                                 <i class="glyphicon glyphicon glyphicon-ok-circle"></i>
                             </label>

                         </div>
                     }

                    <script type="text/javascript">
                         $(function () {
                             $(':radio[name="verlanglijst"]').change(function () {
                                 $.ajax({
                                     url: '@Url.Action("AddOfVerwijderVerlanglijst", "Catalogus", new { id = item.ProductId })',
                                     type: 'POST',
                                     data: { "actie": $('radio[name="actie"]:checked').val(),
                                     success: function (result) {
                                         //alert("success");
                                         console.log("click");
                                     },
                                     error: function (result) {
                                         alert("error!");
                                     }
                                 });
                             });
                         });
                     </script>*@
                    <label class="btn  verlanglijstlabel">
                        <span class="glyphicon verlanglijstglyp"></span>

                        @{
                            var url = Url.Action("AddOfVerwijderVerlanglijst", new { id = item.ProductId });
                        }
                        @Html.CheckBoxFor(modelItem => item.InVerlanglijst, new { id = item.ProductId, onclick = "window.location = '" + url + "'", @class = "toggle" }) toevoegen
                    </label>
                    @*<a href="@Url.Action("AddToVerlanglijst", new {id = item.ProductId})" class="btn btn-default">

                        <span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span>
                    </a>*@
                    
                </td>
            </tr>
        }

    </table>
</div>
<div class="visible-sm visible-xs">
    @if (!Model.Any())
    {

        <h4>Geen producten gevonden</h4>

    }
    @foreach (var item in Model)
    {
        

        <div class="row ">

            <div class="col-xs-12 product-small">
                <div class="foto col-xs-4">
                    <img alt="@Html.DisplayFor(modelItem => item.Naam)" src="~/Content/images/@(@Html.DisplayFor(modelItem =>item.Foto))" class="img-responsive"/>
                </div>
                
                <div class="caption col-xs-8">
                    <h2>@Html.DisplayFor(modelItem => item.Naam) 
                    @using (Html.BeginForm("AddToVerlanglijst", "Catalogus", new { id = item.ProductId }))
                    {
                        <button type="submit" class="btn btn-default ">
                            <span class="glyphicon glyphicon glyphicon-ok-sign"></span>
                        </button>
                    }
                </h2> 
                   
                    <p>@Html.DisplayFor(modelItem => item.Omschrijving)</p>
                   
                    <span class="pull-left">@Html.ActionLink("Details", "Details", new {id = item.ProductId })</span><span class="pull-right">Aantal: @Html.DisplayFor(modelItem => item.Aantal)</span>
                </div>
            </div>
            <br />
        </div>
        
    }
            </div>
